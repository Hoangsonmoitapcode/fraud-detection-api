# Production requirements - optimized for Railway
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9
alembic==1.12.1
pydantic>=2.5.0
psutil>=5.9.0

# Lighter ML alternatives for production
torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu
transformers>=4.20.0
scikit-learn>=1.0.0

# Optional: Use CPU-only versions to reduce build time
# torch==2.8.0+cpu --index-url https://download.pytorch.org/whl/cpu
# torchvision==0.19.0+cpu --index-url https://download.pytorch.org/whl/cpu
